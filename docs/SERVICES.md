## Services Installation Guide v.0.1

### Prerequisites

    $> apt install gradle
    $> apt install npm
    $> apt install nodejs-legacy
    $> npm install phantomjs-prebuilt
    $> npm install -g grunt-cli